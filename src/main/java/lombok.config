# lombok에서도 Qualifier가 사용 가능하도록
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier

# 이 설정이 있어야 Lombok이 필드의 @MainDiscountPolicy 어노테이션을 생성자 파라미터로 복사합니다.
lombok.copyableAnnotations += com.example.spring_study.annotation.MainDiscountPolicy